services:
  mirae-tts-server:
    build:
      context: .
      target: runtime
    image: mirae-tts-server:latest
    ports:
      - "3000:3000"
    restart: always
    environment:
      LISTEN: "0.0.0.0:3000"
      MAXIMUM_LENGTH: "0"
