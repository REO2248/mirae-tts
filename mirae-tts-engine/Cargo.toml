[package]
name = "mirae-tts-engine"
version = "0.1.0"
edition = "2024"
description = "Mirae 2.0 TTS engine reimplementation in Rust (byte-exact port)"
license = "MIT"

[lints.rust]
unsafe_code = "forbid"

[dependencies]
